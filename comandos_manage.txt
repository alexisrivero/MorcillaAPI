createsuperuser = crea un usuario par a el panel de administracion de django. se necesita base de datos migrada para que funcione
check = checkeo general de sintaxis y base de datos
compilemessages = crear archivos de internacionalizacion(ej cuando tengamos la pp de varios idiomas te genera un archivos de idiomas)
createcachetable = crea las tablas de cache en base a la configuracion determinada
dbshell = permite ejecutar una linea de sql en el cliente que esta configurado en settings
diffsettings = compara diferencias entre el settings.py actual y el default
dumpdata = permite volcar todos los datos de tu base de datos a un archivo
flush = rearma la base de datos
inspectdb = chusmear una base de datos y mostrarte los modelos que contiene
loaddata = carga en la base de datos desde un fixture/json de datos
makemessages = vale verga
makemigrations = hacer las migraciones de los cambios en los modelos
migrate = aplica las migraciones pendientes
sendtestemail = manda mail de prueba
shell = es una terminal de python con todos los modelos de django cargados
showmigrations = muestra las migraciones
squashmigrations = NO USAR(por ahora primero ser jedi)
startapp = agrega una aplicacion a un proyecto
starproject = crea un proyecto nuevo
test = ejecuta las pruebas de unidad 
testserver = corre un server de django con tu proyecto y los datos de prueba que vos le proveas.
clearsessions = limpia las sesiones
collectstatic = agarra todos los archivos estaticos o binarios del proyecto y los pone en la carpeta configurada para archivos estaticos
runserver = arranca un servidor de prueba.